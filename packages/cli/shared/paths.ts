import { resolve } from 'path'

export const DIR_ROOT = resolve(__dirname, '..', '..', '..')

export const DIR_DOCS = resolve(DIR_ROOT, 'docs')

export const DIR_COMPS = resolve(DIR_ROOT, 'packages/components')

export const DIR_HOOKS = resolve(DIR_ROOT, 'packages/hooks')
